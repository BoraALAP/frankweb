
#endpoint: ${env:PRISMA_PRODENDPOINT}
endpoint: https://frank-yoga-prod-d0e2df7bfd.herokuapp.com/frankProd/prod
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}

hooks:
  post-deploy:
    - graphql get-schema -p prisma

    